"""Configuration for django_app."""

from django.apps import AppConfig


class DjangoAppConfig(AppConfig):
    """Configuration for django_app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "django_app"
